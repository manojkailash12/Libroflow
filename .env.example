# MongoDB Atlas Connection
# Get this from MongoDB Atlas: Clusters → Connect → Connect your application
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/libroflow

# JWT Secret Key
# Generate a random 32+ character string for security
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-to-something-random-and-secure

# Email Configuration (Gmail SMTP)
# For Gmail: Enable 2-Step Verification, then generate App Password
# Go to: https://myaccount.google.com/apppasswords
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=libroflow8@gmail.com
EMAIL_PASSWORD=afha ifwi grgh kequ

# Library Settings
LOAN_PERIOD_DAYS=14
FINE_PER_DAY=10
CURRENCY=INR
CURRENCY_SYMBOL=₹
BOOK_DEPOSIT=50
FEEDBACK_URL=https://your-site.netlify.app/feedback
